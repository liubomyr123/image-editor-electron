import React from 'react';

import './index.scss';

const Header = () => {
    return (
        <header className="app-header">
            Image Editor
        </header>
    );
};

export default Header;
